# gcd

Greatest Common Divisor (GCD) implementation using the Euclidean algorithm with tests.

Files

- `gcd.go` — `GCD(a, b int) int` implementation.
- `gcd_test.go` — unit tests.

Usage

- Run tests: `go test ./gcd`.
- Import `review-go/gcd` to use `gcd.GCD`.

