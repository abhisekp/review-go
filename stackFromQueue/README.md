# stackFromQueue

Stack implementation using two queues. Provides `Push`, `Pop`, `Peek`, and `Values` iteration.

Files

- `stack.go` — implementation using two `queue.Queue` instances.

Usage

- Create: `s := stack.NewStack[int]()` and use `s.Push(1)`, `s.Pop()`, `s.Peek()`.

