# lib

Example interfaces and concrete implementations demonstrating embedding, construction, and method overriding.

Files

- `lib.go` — `IBase`, `IConcrete`, `base`, and `Concrete` example implementations and constructors.

Usage

- Import `review-go/lib` and construct examples using `NewConcrete()` to explore the pattern.

