# utils

Collection of helper functions and generators: fibonacci, n-nacci, primes, partition utilities, selection sort helpers, and more.

Files of interest

- `fibs.go`, `primes.go`, `partition.go`, `selectionSort.go`, `sum.go`, etc.

Usage

- Import `review-go/utils` for generator helpers and utility functions used across the repo.

