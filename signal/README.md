# signal

Examples demonstrating OS signal handling: a manual channel-based approach and a `context`-based approach using `signal.NotifyContext`.

Files

- `signal.go` — `Signal()` and `SignalCtx()` examples that wait for interrupts.

Usage

- Run the package example with `go run ./signal` or import and call `signal.SignalCtx()` from your program.

