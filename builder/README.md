# builder

Example demonstrating interface embedding, the builder pattern, and base/derived construction patterns in Go.

Files

- `base.go` — Base builder implementation and helper methods.
- Other files — concrete builder implementations and types.

Usage

- Import and use the builders or run small examples that call constructors and `Setup` / `Print` methods.

