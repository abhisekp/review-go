# node

Generic `Node[T]` used by linked list and other structures. Provides thread-safe accessors and mutators for `value`, `next`, and `prev`.

Files

- `node.go` — `Node[T]` implementation and constructor `NewNode`.

Usage

- Import `review-go/node` to build custom linked structures or use with `linkedlist`.

