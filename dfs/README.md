# dfs

Depth-first traversal utilities and a `FileNode` type used to represent directories/files. The package currently contains a skeleton for DFS traversal.

Files

- `dfs.go` — `FileNode` type and `DFS` struct (some functions may be placeholders).

Usage

- Import `review-go/dfs` and use or extend the `DFS` type for custom traversal logic.

