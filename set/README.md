# set

Generic set implementation with union, intersection, difference, and symmetric difference helpers.

Files

- `set.go` — implementation.

Usage

- Create: `s := set.NewSet[int](1,2,3)` and use `s.Add`, `s.Contains`, `set.Union(s1, s2)`.

